// we can write the below thing in babelrc file also here in normal obj formal, there it's json format.

// module.exports = {
//   presets: [["@babel/preset-env", { targets: { node: "current" } }]],
// };
